SELECT
    FILIAL,
    IDNOTA,
    IDFATURA,
    DATAVENCIMENTO,
    QTDEDIAS,
    VALORFATURA,
    IDTITULO
FROM
    FATURASPEDCOMPRA
    /*
WHERE
    FATPED.FILIAL = ? AND
    FATPED.IDNOTA = ?
    */

select 
    cadastroparceiros.nome,
    fornecedores.* 
from 
    fornecedoresitem 
inner JOIN
    cadastroparceiros on
        cadastroparceiros.cadastro=fornecedoresitem.FORNECEDOR
where 
    fornecedoresitem.iditem='05021'
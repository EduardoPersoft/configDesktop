SELECT 
    COMPOSICAOITEM.IDITEM,
    ITEMESTOQUE.CODIGO,
    coalesce(itemestoque.itembase, COMPOSICAOOP.CODIGO),
    ITEMESTOQUE.DESCRICAO
FROM 
    COMPOSICAOITEM
INNER JOIN
    ITEMESTOQUE ON
        (ITEMESTOQUE.CODIGO=COMPOSICAOITEM.IDMATERIAL)
WHERE
    COMPOSICAOITEM.IDITEM = '000005'
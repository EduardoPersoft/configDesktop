--insert into
    --ITEMESTOQUECODIGOS
select
    DISTINCT
    ITEMESTOQUECODIGOS.CODIGO,
    ITEMESTOQUECODIGOS.CODIGO
from
    C_ITEMESTQOUTCOD
where 
    NOT EXISTS 
    (SELECT 
        * 
     FROM 
        ITEMESTOQUECODIGOS
     WHERE   
        C_ITEMESTQOUTCOD.CODIGOALTERNATIVO=ITEMESTOQUECODIGOS.CODIGO)
select    
    1 as id,
    extract(weekday from orcamento.DATACADASTRO) as qtd,
    count(orcamento.idnota)    
from
    orcamento
where
    orcamento.datacadastro>cast('now' as date)-extract(weekday from cast('now' as date)) and
    orcamento.dataCancelamento is null
group by
    extract(weekday from orcamento.DATACADASTRO)

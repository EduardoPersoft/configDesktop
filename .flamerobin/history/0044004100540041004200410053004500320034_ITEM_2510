SELECT 
    a.IDITEM, 
    a.SEQUENCIA,     
    a.IDMATERIAL, 
    a.QUANTIDADE, 
    a.QUANTIDADESETUP,
    a.CENTROCUSTO
FROM 
    COMPOSICAOITEM a 
inner join
    itemestoque materiaprima on
        materiaprima.codigo=a.idmaterial
WHERE
    a.IDITEM = '?'

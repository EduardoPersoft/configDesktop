SELECT
    CODIGO,
    CODIGOBARRA,
    DESCRICAO,
    LOCALIZACAO
FROM
    ITEMESTOQUE
INNER JOIN
    MATERIALLOCAL ON
        MATERIALLOCAL.IDITEM = ITEMESTOQUE.CODIGO
WHERE
    MATERIALLOCAL.SALDOTOTAL>0
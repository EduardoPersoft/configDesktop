SELECT 
    USUARIO.ID,
    USUARIO.USUARIO,
    COALESCE(USUARIO.PERFIL,usuario.id),
    VENDEDOR.CADASTRO ,
    vendedor.
    USUARIO.PATH
FROM 
    USUARIO
LEFT JOIN 
    VENDEDOR ON 
        VENDEDOR.USUARIORELACIONADO = USUARIO.ID

    

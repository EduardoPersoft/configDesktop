select
    CADASTROPARCEIROS.CADASTRO,
    CADASTROPARCEIROS.NOME,
    MAX(PEDIDO.DATACADASTRO) DATA
from
    PEDIDO
INNER join
    CADASTROPARCEIROS ON
        CADASTROPARCEIROS.CADASTRO=PEDIDO.DESTINATARIO
where 
    pedido.filial=110
GROUP BY
CADASTROPARCEIROS.CADASTRO,
    CADASTROPARCEIROS.NOME
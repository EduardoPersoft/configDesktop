SELECT
   titulo,
   fatura,
   valortitulo,
   cast(substring(observacoes from 1 for 1000) as varchar(1000)) as observacoes
FROM
    TITULOPAG
where 
    titulopag.filial=5 AND
    titulopag.parceiro=19446
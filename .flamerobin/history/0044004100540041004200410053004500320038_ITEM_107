update
    CADASTROPARCEIROS
set
    PARCEIROCOLABORADOR = CASE WHEN 
        EXISTS (
            select 
                *
            from
                COLABORADORES
            where
                COLABORADORES.CADASTRO=CADASTROPARCEIROS.CADASTRO)        
    then 'S' else 'N' end
update
    CADASTROPARCEIROS
set
    PARCEIROCLIENTE = CASE WHEN 'S'
        EXISTS (
            select 
                *
            from
                CLIENTES
            where
                CLIENTES.CADASTRO=CADASTROPARCEIROS.CADASTRO)        
    else 'N' end
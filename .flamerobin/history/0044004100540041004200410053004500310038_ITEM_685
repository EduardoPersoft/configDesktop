 select
    distinct
    p_usuario.usuario perfil
from
   usuario u_usuario
left join
   usuario p_usuario on
     u_usuario.perfil = p_usuario.id
where
    u_usuario.tipo = 'U' 
    
order by
    1
    

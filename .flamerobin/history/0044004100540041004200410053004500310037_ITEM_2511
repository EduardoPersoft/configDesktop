select
    orcamento.datacadastro,
    extract(weekday from orcamento.DATACADASTRO),
    count(orcamento.idnota)    
from
    orcamento
where
    orcamento.datacadastro>cast('now' as date)-extract(weekday from cast('now' as date))
group by
    orcamento.datacadastro,
    
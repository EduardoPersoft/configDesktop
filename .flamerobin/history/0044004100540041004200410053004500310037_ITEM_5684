insert into c_fretecep(cep,valorfrete)
select
    CADASTROPARCEIROS.CEP,
    MAX(C_FRETECLIENTE.VALOR)
from
    C_FRETECLIENTE
inner join
    CADASTROPARCEIROS ON
        CADASTROPARCEIROS.CADASTRO=C_FRETECLIENTE.ID
where
    cadastroparceiros.cep is not null
GROUP BY 
    CADASTROPARCEIROS.CEP
SELECT TRANSPORTADORA.CADASTRO,cadastroparceiros.NOME,cadastroparceiros.telefone
FROM TRANSPORTADORA
INNER JOIN CADASTROPARCEIROS ON (TRANSPORTADORA.CADASTRO=CADASTROPARCEIROS.CADASTRO)
ORDER BY CADASTROPARCEIROS.cadastro

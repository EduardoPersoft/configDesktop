SET TERM ^;

CREATE TRIGGER C_ITEMFILIAL FOR ITEMESTOQUE
ACTIVE BEFORE INSERT OR UPDATE POSITION 0
AS
BEGIN
    /* enter trigger code here */ 
    IF (SUBSTRING(NEW.CODIGO FROM 1 FOR 1)='6') then
        NEW.FILIALPADRAO = 12826;
    ELSE
        NEW.FILIALPADRAO = 1;
    
END^

SET TERM ;^

SELECT 
    r.CODIGO, 
    r.DESCRICAO, 
    r.CODIGONOVO, 
    r.CODIGOBASE,
    (select  FIRST 1
        INVENTARIOESTOQUEDATA.VALOR
     from
        INVENTARIOESTOQUEDATA
     where
        INVENTARIOESTOQUEdata.CODIGO=r.CODIGOBASE and
        COALESCE(INVENTARIOESTOQUEDATA.VALOR,0)>0
     order by
        INVENTARIOESTOQUEDATA.DATA DESC)
        
FROM 
    C_CONVERSAOMP r
CREATE TABLE ACESSOS
(
  PATH varchar(250) NOT NULL,
  CONSTRAINT PK_ACESSOS PRIMARY KEY (PATH)
);
CREATE TABLE REGRAS
(
  ID integer NOT NULL,
  PATH varchar(250) NOT NULL,
  ACESSO smallint,
  CONSTRAINT PK_REGRAS PRIMARY KEY (ID,PATH)
);

update
    CADASTROPARCEIROS
set
    PARCEIROFORNECEDOR = CASE WHEN 
        EXISTS (
            select 
                *
            from
                FORNECEDOR
            where
                FORNECEDOR.CADASTRO=CADASTROPARCEIROS.CADASTRO)        
    then 'S' else 'N' end
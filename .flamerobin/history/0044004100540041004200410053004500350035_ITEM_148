select 
    CADASTROPARCEIROS.cadastro,
    CADASTROPARCEIROS.nome,
    CADASTROPARCEIROS.telefone,
    CADASTROPARCEIROS.email,
    CADASTROPARCEIROS.logradouro,
    CADASTROPARCEIROS.PRINCIPALCONTATO
from
    VENDEDOR
inner join
    cadastroparceiros on
        cadastroparceiros.cadastro=vendedor.CADASTRO
where
    vendedor.TIPOVENDEDOR=5 and (not cadastroPARCEIROS.nome like '%LGPD%')
order by
    CADASTROPARCEIROS.nome
SELECT 
    CADASTROPARCEIROS.C_CODCLIENTEANTIGO,
    COUNT(CADASTROPARCEIROS.CADASTRO)
FROM 
    CADASTROPARCEIROS
GROUP BY 
    CADASTROPARCEIROS.C_CODCLIENTEANTIGO
HAVING
    COUNT(CADASTROPARCEIROS.CADASTRO)>1    